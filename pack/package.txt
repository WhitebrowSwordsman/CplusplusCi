if(CPACK_GENERATOR MATCHES "ZIP")
  message(STATUS "cpack generator is zip")
elseif(CPACK_GENERATOR MATCHES "NSIS")
  message(STATUS "cpack generator is nsis")
else()
  message(STATUS "cpack generator is unknown")
endif()